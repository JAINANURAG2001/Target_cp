// find the index of the minumum element of the subarray of size k 


#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, k;
    cin >> n >> k;
    vector<int> v(n);
    for (auto &i : v)
    {
        cin >> i;
    }

    // method 1 using the set and after that add {arr[i], i } 




    return 0;
}